{
  "name": "node-app",
  "version": "1.0.0",
  "description": "Simple Node.js app deployed on AWS EKS via Kubernetes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
